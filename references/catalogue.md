# Catalogue

Use `references/INDEX.md` (human) or `corpus/index.json` (machine).
Do not load every transcript.

Each row has: YouTube id, title, URL, topic tags, word count, path to
that video's `.txt` and `.srt`.
